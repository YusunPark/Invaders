pipeline {
    agent none

    stages {
        stage('build') {
            steps {
                echo 'jenkins sample build ! %{AUTHOR}'
            }
        }
    }

    post {
        always {
            echo 'post process !'
        }
    }

}

